"""
[동적 프로그래밍 - 피보나치 수열 (하향식 / Top-down)]

문제 설명:
- 메모이제이션(Memoization)을 사용한 하향식 DP로 피보나치 수를 계산합니다.
- 이미 계산한 값을 저장하여 중복 계산을 방지합니다.
- Week1의 재귀 피보나치(O(2^n))를 DP로 최적화(O(n))합니다.

입력:
- n: 구하고자 하는 피보나치 수의 인덱스

출력:
- n번째 피보나치 수

예제:
입력: n = 10
출력: 55

힌트:
- 딕셔너리에 계산 결과 저장
- 이미 계산했으면 저장된 값 반환
- 계산 안 했으면 재귀 호출 후 저장

설명:
- 시간 복잡도: O(n) - 각 값을 한 번만 계산
- 공간 복잡도: O(n) - memo 딕셔너리와 재귀 스택
- Week1 재귀: O(2^n) → DP: O(n) 로 극적인 성능 향상! 이런게 가능하다니... 

동적 프로그래밍 (Dynamic Programming):
- 큰 문제를 작은 부분 문제로 나누어 해결
- 부분 문제의 결과를 저장하여 재사용 : 이것이 DP의 핵심 포인트
- 중복 계산을 제거하여 효율성 향상

하향식 (Top-down) 방식:
- 큰 문제부터 시작
- 재귀로 작은 문제로 분해
- 메모이제이션으로 중복 제거
- fib(10) → fib(9) + fib(8) → ... → fib(0), fib(1)

메모이제이션 (Memoization):
- 계산 결과를 memo에 저장
- 같은 값을 다시 계산할 필요 없음
- 캐싱(Caching)과 유사한 개념

계산 과정 (예: fib(5)):
일반 재귀:
                    fib(5)
                 /          \
            fib(4)            fib(3)
           /      \          /      \
       fib(3)   fib(2)   fib(2)   fib(1)
       ...      ...      ...       ...
총 계산: 15번 (중복 다수)

메모이제이션:
fib(5) 계산
→ fib(4) 계산 (저장)
→ fib(3) 계산 (저장)
→ fib(2) 계산 (저장)
→ fib(1), fib(0) 반환
→ 이후 fib(3), fib(2) 등은 memo에서 꺼내기만
총 계산: 6번 (중복 0)

DP가 필요한 경우:
1. 최적 부분 구조: 부분 문제의 최적해로 전체 최적해 구
2. 중복 부분 문제: 같은 문제가 반복적으로 등장

응용:
- 경로 찾기 문제
- 최적화 문제
- 조합 최적화
- 자원 할당
"""
"""
문제 리뷰
1. Claude가 준 문제에서는 top-down을 할 때 Memo를 함수 밖에 두는 방식과 매개변수로 쓰는 방식을 비교해보자.

1-1 Memo를 함수 바깥에 두는 방식은, 내 생각엔 전역변수를 함수 안에서 수정하는 셈이니 안 될 것 같다.
근데 왜 실제 코드에선 돌아가는가?  
!!! 변수 이름 자체를 다시 묶는 것(rebinding)과 그 변수가 가리키는 객체 내부를 바꾸는 것(mutation)은 다름.

파이썬 변수는 "상자"가 아니라 "이름표"다.
이름표가 가리키는 상자 내부를 바꾸는 건 가능하고,
이름표 자체를 새 상자에 다시 붙이는 건 별개의 일이다.
mutable 객체는 그 객체 자체를 바꿀 수 있음. 그래서 함수 안에서 리스트 내부를 건드리는 것은, 이름표를 새로 만드는ㄱ ㅓㅅ이 아니라
기존 객체를 수정하는 것이라 가능함.

다만 Immutable은 값을 바꾸는게 불가능하기 때문에 결국 새 객체를 만들고 이름을 다시 연결해야 함.

2. 언제 Top-down을 쓰고, 언제 bottom-up을 써야하는가?
!!! 우선 실전에서는 bottom-up을 더 많이 씀. 
왜? 재귀가 없다는게 주된 이유임. recursion error 뜰 일 없고 메모리 효율도 좋고, 코드가 직관적임

Top-down이 유리한 경우는 
부분 문제 중 일부만 필요할때, 점화식은 알겠는데 순서가 복잡할 때 등이 있음
반면 Bottom-up은 순서가 명확할 때(1->2->3.. ) 재귀가 깊어질때

기본 마인드 셋 : 전부 계산해야 하면 Bottom-up, 일부만 필요하면 top-down, 잘 모르겠으면 Bottom-up


3. if i in memo와 if memo[n]의 차이점

핵심 차이는 key 없이 접근했을 때. If i in memo는 False, if memo[n]은 오류!

!!! if i in memo로 가는게 안전함.
지금 이 문제에서는 memo가 기존에 없는 값, 즉 새로 계산해야 하는 값에서 모두 오류가 떠버려서 i in memo가 맞음

??? 그러면 value를 찾을때는? 10 in memo.values() 이런식으로. 다만, value는 전부 뒤져야 하므로 O(n)

4. if not memo와 if memo is None의 차이점
!!!전자는 비었거나[]{} "" 값이 0일때, None일때 모두 충족하나, 후자는 오직 값이 None일때만 충족함

"""
def fibonacci_memo(n, memo=None): #값을 갖고 내려가려고 매개변수로 받는구나. ??? 근데 다른 방식도 가능할 것 같은데? 아 이거 어제 봤던 전역변수임에도 불구하고 추가가 됐던 이유 그 부분이구나. 문제 풀고 나서 다시 한 번 파보자
    """
    메모이제이션을 사용한 피보나치 (하향식 DP)
    
    Args:
        n: 피보나치 인덱스
        memo: 계산 결과를 저장할 딕셔너리
    
    Returns:
        n번째 피보나치 수
    """
    # TODO: memo가 None이면 빈 딕셔너리로 초기화
    if memo is None :
        memo={}
    
    # TODO: base case 
   
    if n == 0:

        memo[n] = 0 #왜 이게 0까지 내려가지? => 아.. 2일때 1과 0을 호출하는구나. 
    
    if n==1 :
        
        memo[n] = 1
        # return memo[n] # 이 줄도 아래 if memo[n] 조건이 있으니 필요가 없네 
   
    if n == 2 :
        memo[n] = 1
    
    # TODO: 이미 계산한 값이 memo에 있으면 반환
    if n in memo : 
        return memo[n]
    
    # TODO: 재귀 호출하여 계산하고 memo에 저장
    memo[n] = fibonacci_memo(n-1, memo) + fibonacci_memo(n-2, memo)
    
    return memo[n]

# 테스트 케이스
if __name__ == "__main__":
    # 테스트 케이스 1
    print("=== 피보나치 수열 (메모이제이션) ===")
    for i in range(11):
        result = fibonacci_memo(i)
        print(f"fib({i}) = {result}")
    print()
    
    # 테스트 케이스 2: 큰 수도 빠르게 계산
    print("=== 큰 수 계산 ===")
    n = 50
    result = fibonacci_memo(n)
    print(f"fib({n}) = {result}")
    print()
    
    # 비교: Week1의 재귀 방식은 fib(50)을 계산하기 어려움
    print("참고: 일반 재귀는 fib(40)도 몇 초 걸리지만")
    print("메모이제이션은 fib(100)도 순식간에 계산!")


